{% extends 'base.html.twig' %}

{% block body %}
<div class="container">
  <div class="row">

<div class="alert alert-success">
      <h1>Mes brouillons </h1>
  <a href="{{ path('post_index') }}"> <button type="button"><i class="fa fa-undo" aria-hidden="true"></i> Retour</button></a>
</div>

        {% for post in posts %}
        <div class="well col-md-12">
          <button type="button" name="button"><i class="fa fa-trash" aria-hidden="true"></i> Supprimer</button>
            <a href="{{ path('post_edit', { 'id': post.id }) }}"> <button type="button" name="button"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Modifier</button></a>
          <button type="button" name="button"><i class="fa fa-file" aria-hidden="true"></i> Mettre en ligne</button>
            <h2 class="col-md-12"> {{ post.titre }} </h2>
            <i class="col-md-12">*Publié le {% if post.createAt %}{{ post.createAt|date('d-m-Y') }}{% endif %}{% if post.updateAt %}, Modifié le{{ post.updateAt|date('d-m-Y') }}{% endif %}</i>
            <img class="img-responsive col-md-6 col-md-offset-3"src="img/placeholder.png" alt="" />
            <p class="col-md-12">{{ post.contenu }}</p>
          </div>
          {% endfor %}

        </div><!--Row -->
        </div><!--container-->

{% endblock %}
